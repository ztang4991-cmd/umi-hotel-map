# UMI 酒店地图项目

> 这是给 Claude Code 的项目记忆文件。开始任何任务前请先通读此文档，理解业务背景、已交付内容、数据结构和待办事项。

---

## 一、项目概述

**项目名称**：UMI 酒店地图工作台

**一句话定义**：给婚礼管家用的客户需求匹配工具，把客户挖需到推荐酒店的链路从"凭脑子"变成"靠工具"。

**产品形态**：
- **底层数据库**：Excel 主数据库（80 家酒店，13 个字段）
- **管家工具**：单文件 HTML 网页，含交互式高德地图 + 4 维度筛选
- **培训材料**：Word 文档，给新管家入职用

**用户**：公司内部婚礼管家（约 10-30 人规模）

---

## 二、业务背景

### 团队来源
团队来自婚礼纪（国内头部婚礼平台），有丰富的酒店资源和返佣业务认知。

### 商业模式（核心链路）
```
小红书挖客 → 小红书运营分配客资至管家 → 管家挖需 → 匹配合作渠道/酒店 → 客户签约 → 渠道商返佣
```

### 合作渠道商（2 个）
| 渠道商 | 代理酒店数 | 颜色标识 | 备注 |
|--------|-----------|---------|------|
| 时光在线 | ~75 家 | 🔵 蓝色 | 返佣合作 |
| 昌森 | ~10 家 | 🟣 紫色 | 返佣合作 |
| 双渠道重叠 | 5 家 | 🔴 红色 | 两边都能下单，优先推荐 |

**重叠的 5 家酒店**：滨江希尔顿逸林、龙之梦、大鼎世纪、凯宾斯基、东安阁

---

## 三、当前状态（V1.0 已交付）

### 已完成
- ✅ 87 条原始酒店数据清洗 → 80 家有效酒店（删除停业、合并重复、补充多门店）
- ✅ 所有酒店 GCJ-02 经纬度（通过高德地理编码 API 批量获取，成功率 100%）
- ✅ 风格标签初版（30+ 家手动分类，其余默认"酒店宴会厅"）
- ✅ HTML 地图工具（高德底图 + 4 维度筛选 + 渠道颜色区分）
- ✅ Excel 主数据库（13 个字段，含中文格式化）
- ✅ 婚礼管家培训手册（Word，11 章节，含 5 道考核题）

### 数据完整度
| 字段 | 完整度 | 说明 |
|------|--------|------|
| 基础信息（名称/地址/区域） | 100% | 完整 |
| 经纬度 | 100% | 已通过高德 API 全部获取 |
| 风格标签 | ~40% 手动 | 其余默认值，需管家实地后修正 |
| 餐标档位 | ~10% | 大量"待询"，需管家拓客时回填 |
| 桌数档位 | ~15% | 同上 |
| 宴会厅参数（层高、面积等） | ~20% | 来自前期搜索的公开资料 |

---

## 四、项目文件清单

### 数据文件
- `data/UMI酒店主数据库_V1.xlsx` — **核心数据源**（80 家酒店主数据表）
- `data/UMI_酒店坐标_2026-05-19.xlsx` — 原始 87 条坐标查询结果
- `data/UMI_坐标补查_2026-05-19.csv` — 四方阁多门店 + 朗丽兹补查结果
- `data/UMI星级酒店宴会厅参数表_婚礼管家版.xlsx` — 前期收集的宴会厅详细参数（参考用）

### 工具文件
- `tools/UMI酒店地图_V1.html` — **管家用的地图工具**（单文件 HTML，浏览器直接打开）
- `tools/UMI酒店坐标查询工具.html` — 批量地理编码工具（拿到高德 Key 后浏览器跑）
- `tools/UMI_坐标补查工具.html` — 单次补查工具（特殊情况用）

### 文档文件
- `docs/UMI酒店地图_管家培训手册.docx` — 给管家的培训文档
- `CLAUDE.md` — 本文件（Claude Code 项目记忆）
- `README.md` — 人类可读的项目说明

---

## 五、数据架构

### 主数据库字段（13 列）

| 字段 | 类型 | 说明 | 取值示例 |
|------|------|------|---------|
| 编号 | int | 主键 | 1, 2, 3... |
| 酒店名称 | string | 酒店全称 | "成都棕榈泉费尔蒙酒店" |
| 合作渠道 | enum | 渠道归属 | "时光在线"/"昌森"/"两者" |
| 原区域 | string | 行业习惯叫法 | "高新区"、"天府新区" |
| 行政区(高德) | string | 真实行政区划 | "武侯区"、"双流区" |
| 详细地址 | string | 完整地址 | "武侯区天府大道..." |
| 经度 | float | GCJ-02 经度 | 104.0513 |
| 纬度 | float | GCJ-02 纬度 | 30.6645 |
| 是否在地图显示 | bool | 隐藏标记 | true/false |
| 风格标签 | string | 1-2 个标签用 / 分隔 | "西式 / 酒店宴会厅" |
| 餐标档位 | enum | 4 档 + 待询 | "3000-5000"、"待询" |
| 桌数档位 | enum | 4 档 + 待询 | "50-100桌"、"待询" |
| 高德匹配名称 | string | 调 API 返回的酒店名 | "费尔蒙酒店(成都)" |

### 风格标签体系（6 类）
| 标签 | 典型特征 | 客户画像 |
|------|---------|---------|
| 酒店宴会厅 | 标准酒店大堂宴会厅 | 传统体面，最广谱 |
| 中式 | 红绸、雕花、中式园林 | 传统家庭 |
| 西式 | 欧式柱、教堂感、白纱仪式 | 仪式感偏好 |
| 户外草坪 | 草地、白色帐篷、户外仪式 | 浪漫派、夏秋季 |
| 独栋会所 | 整栋包场、私密 | 小众、注重隐私 |
| 园林度假 | 湖畔/山景/度假村 | 文艺、户外控 |

### 餐标档位
- `<1500`（经济型）
- `1500-3000`（中档）
- `3000-5000`（中高档）
- `5000+`（高端）
- `待询`（数据未确认）

### 桌数档位
- `≤20桌`（小型）
- `20-50桌`（中型，最常见）
- `50-100桌`（大型）
- `100桌+`（超大型）
- `待询`

### 区域处理（重要！）

我们用 **双轨制**：
- `原区域`：行业习惯叫法（高新区、天府新区），管家筛选用这个
- `行政区(高德)`：高德 API 返回的真实行政区，地图显示用这个

**为什么这样设计**：
- 高新区是国家级开发区，实际属于武侯区/双流区/郫都区
- 天府新区是省级开发区，实际属于双流区/简阳
- 但管家和客户沟通时都习惯说"高新区"，地图筛选必须支持这种叫法

---

## 六、技术栈

| 层级 | 技术 |
|------|------|
| 数据存储 | Excel (`.xlsx`)，未来可迁移到数据库 |
| 地理编码 | 高德开放平台 Web 服务 API |
| 坐标系 | **GCJ-02**（火星坐标系，中国境内强制） |
| 地图渲染 | Leaflet 1.9.4 + 高德地图 tile 服务 |
| 前端框架 | 纯 HTML + 原生 JS（无构建依赖） |
| 文档生成 | Python (openpyxl) + Node.js (docx) |

### 高德 API
- 地理编码 API：`https://restapi.amap.com/v3/geocode/geo`
- POI 搜索 API：`https://restapi.amap.com/v3/place/text`
- 地图 tile：`https://wprd0{s}.is.autonavi.com/appmaptile?...`
- **API Key 保存在 `.env` 或单独的安全位置**（已开通免费版 Web 服务，每天 30 万次调用额度）

---

## 七、关键决策记录（重要）

### 决策 1：为什么用高德不用谷歌
**结论**：长期产品形态必须是【高德/百度底图 + GCJ-02 坐标】

**原因**：
1. **坐标系**：中国境内必须用 GCJ-02（国家测绘法），谷歌返回 WGS-84 有 50-500m 强制偏移
2. **数据覆盖**：高德覆盖国内 POI 完整，谷歌在大陆 POI 覆盖差，本土小品牌大概率查不到
3. **底图可访问性**：谷歌地图国内访问慢/被墙，管家用户体验差

### 决策 2：跨渠道重复酒店处理
**结论**：合并去重，加"合作渠道"字段标识

**实现**：
- 5 家重叠酒店在主表中只保留一条
- "合作渠道"字段值改为"两者"
- 地图上用红色点位特殊标识，管家优先推荐

### 决策 3：档期筛选 V1 不做
**结论**：档期是动态数据（天级变动），地图工具无法维护准

**未来策略**：用户提到会做"档期共享表"，作为独立项目处理

### 决策 4：返佣比例 / 渠道优先级处理
**结论**：V1 不处理，作为独立项目

**未来**：可能加"渠道优先级"或"佣金率"字段，影响管家选哪边下单

### 决策 5：金堂美景阁标记隐藏
**原因**：原数据中"金堂美景阁"在高德 API 中匹配到了青羊区的同名餐厅（不是想要的金堂酒店）
**处理**：`是否在地图显示 = false`，保留数据但不显示

### 决策 6：删除的酒店
- **#75 成都三岔湖环球融创天堂洲际酒店**：暂停营业
- **#63 成都空港大酒店**：实际就是 #66 空港国际会议中心，合并去重

---

## 八、待办与路线图

### 短期（V1.1，可立即开始）
- [ ] **回填"待询"数据**：管家在拓客时拿到酒店餐标/桌数后，按"数据回流机制"提报，运营汇总更新
- [ ] **风格标签精修**：管家实地带看后修正标签
- [ ] **新增酒店流程**：标准化新酒店进库的字段填写要求
- [ ] **数据回流群+表单**：建企业微信群"UMI 酒店数据更新群"，定义回流格式（培训文档已写）

### 中期（V2，1-3 个月）
- [ ] **档期共享表**：和酒店对接档期信息（独立项目）
- [ ] **渠道关系处理**：佣金比例、优先级字段（独立项目）
- [ ] **酒店详情页**：点击酒店弹出更详细的卡片（含照片、宴会厅参数、联系人）
- [ ] **移动端适配**：管家电话沟通时手机随时查
- [ ] **嵌入内部系统**：和现有 CRM 集成

### 长期（V3，6 个月+）
- [ ] **多城市扩展**：成都之外的其他城市
- [ ] **客户画像匹配**：基于历史成交数据训练推荐算法
- [ ] **AI 辅助挖需**：客户对话实时翻译成筛选条件

---

## 九、常见任务 SOP

### 任务 A：添加新酒店到数据库

**前置条件**：拿到酒店名称、地址、合作渠道、星级

**步骤**：
1. 用 `tools/UMI酒店坐标查询工具.html` 单独查这个酒店的经纬度（填入高德 Key，加一行酒店数据）
2. 在 `data/UMI酒店主数据库_V1.xlsx` 增加一行，按 13 个字段填写
3. 餐标/桌数没有就填"待询"
4. 重新生成 `tools/UMI酒店地图_V1.html`（用 build 脚本，把 Excel → 地图 HTML）

### 任务 B：回填"待询"数据

**输入**：从酒店宴会销售获得的餐标、桌数等信息

**步骤**：
1. 找到对应酒店在 Excel 中的行
2. 把"待询"替换为对应档位
3. 重新生成地图 HTML

### 任务 C：重新生成地图 HTML

**Python 脚本逻辑**：
```python
# 读 Excel
df = pd.read_excel('data/UMI酒店主数据库_V1.xlsx')
# 过滤显示
df = df[df['是否在地图显示'] == True]
# 转 JS 数据
hotels_json = df.to_json(orient='records', force_ascii=False)
# 注入到 HTML 模板
# 输出 tools/UMI酒店地图_V1.html
```

### 任务 D：批量地理编码（重做坐标）
1. 打开 `tools/UMI酒店坐标查询工具.html`
2. 修改 HTML 里的 `HOTELS` 数组（加酒店）
3. 用浏览器打开，填 Key，点"开始查询"
4. 下载结果 CSV，合并到主数据库

---

## 十、重要约定与术语

### 业务术语
- **挖客**：通过小红书等渠道获取客资
- **挖需**：和客户沟通确认婚礼具体需求
- **管家**：婚礼管家，负责一对一服务客户
- **客资**：客户资源（联系方式 + 初步需求）
- **带看**：带客户实地考察酒店
- **餐标**：每桌酒席的费用标准（不含服务费、酒水）
- **起订桌数**：酒店宴会厅的最小预订桌数
- **返佣**：客户签约后渠道商返给我们的佣金

### 技术约定
- **坐标系**：所有经纬度统一为 GCJ-02，禁止混入 WGS-84
- **编码**：所有文件使用 UTF-8，CSV 导出加 BOM 头（`\uFEFF`）保证 Excel 打开不乱码
- **API Key 安全**：不要把高德 Key 写在公开代码里，用环境变量或 `.env`
- **地图 tile**：用高德的 `wprd0{1-4}.is.autonavi.com` 子域名负载均衡

---

## 十一、注意事项 / 已知陷阱

### ⚠️ 陷阱 1：高德 POI 搜索可能匹配错误
**例子**：搜索"四方阁"会返回多个不同地点的同名餐厅，搜索"金堂美景阁"匹配到了青羊区的"美景阁"。
**对策**：对小众/泛名酒店，用具体地址做地理编码兜底（`/v3/geocode/geo` 接口）。

### ⚠️ 陷阱 2：高德返回的"行政区"和管家叫法不一致
**例子**：高新区酒店实际属于武侯区/双流区/郫都区。
**对策**：双轨制字段（原区域 + 行政区），筛选用原区域。

### ⚠️ 陷阱 3：跨渠道重复酒店
**对策**：同一家酒店两个渠道都代理时，主表只保留一条，渠道字段标"两者"。

### ⚠️ 陷阱 4：浏览器跨域 / CORS
**情况**：高德 Web 服务 API 支持跨域调用（CORS 已开启），可以直接在浏览器 fetch。
**注意**：如果以后改用 Android/iOS Key，会有签名校验，浏览器调不通。

### ⚠️ 陷阱 5：Excel 中文编码
**情况**：Excel 打开 CSV 中文乱码。
**对策**：导出 CSV 时加 UTF-8 BOM（`'\uFEFF'`）。

---

## 十二、对接 Claude Code 的建议

### 如果你要继续这个项目，建议第一步：
1. 通读本文档（CLAUDE.md）
2. 查看 `data/UMI酒店主数据库_V1.xlsx` 了解数据现状
3. 用浏览器打开 `tools/UMI酒店地图_V1.html` 体验工具
4. 看用户最新需求是什么，对照"待办与路线图"找到对应位置

### 常见用户请求 → 你应该做什么
| 用户说... | 你应该... |
|----------|----------|
| "新增 X 家酒店" | 走"任务 A：添加新酒店"流程 |
| "更新 X 酒店的餐标" | 走"任务 B：回填待询数据"流程 |
| "地图加一个新筛选维度" | 修改 HTML 模板的左侧面板 + JS 筛选逻辑 |
| "导出某区域的酒店列表" | pandas 读 Excel，按 `原区域` 过滤导出 |
| "更新培训文档" | 修改 `docs/UMI酒店地图_管家培训手册.docx` 对应章节 |
| "整合到 CRM 系统" | 这是 V2 大项目，需要先和用户讨论范围和接口 |

### 沟通风格建议
- 用户是业务出身（来自婚礼纪），不是开发出身
- 沟通用大白话，技术细节简化解释
- 涉及关键决策时给方案 + 推荐选项，不要让用户自己拍板技术细节
- 用户偏好简洁、可执行的回复，不要过度铺垫

---

## 十三、版本历史

| 版本 | 日期 | 主要内容 |
|------|------|---------|
| V1.0 | 2026-05-19 | 初始版本：80 家酒店主数据库 + 地图工具 + 培训手册 |
| - | - | 待 V1.1 起持续更新 |

---

**最后更新**：2026 年 5 月
**项目负责人**：（请填写）
**Claude Code 使用提示**：本文件是项目记忆，请勿删除。新增重要决策时追加到"关键决策记录"章节。
